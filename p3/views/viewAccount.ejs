<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Account</title>
    <style>
        body {
            font-family: Arial, Helvetica, sans-serif; /* System default sans-serif font */
            padding: 20px;
            background-color: #fff; /* Softer background for contrast */
            margin: 0;
            line-height: 1.6; /* Improves readability */
        }
        
        /* Account Info Section */
        .account-info {
            margin-top: 60px;
            padding: 25px;
            background-color: #dddcdc; /* Clean white background */
            border-radius: 10px; /* Slightly rounded corners */
            box-shadow: 0 6px 15px rgba(0, 0, 0, 0.1); /* Softer shadow for depth */
            max-width: 600px;
            margin: 80px auto;
            position: relative;
            text-align: center;
            border: 1px solid #ddd; /* Subtle border for a structured look */
        }
        
        /* Button container to align buttons horizontally */
        .button-container {
            display: flex;
            justify-content: space-between; /* Distribute buttons evenly */
            margin-top: 25px;
            gap: 10px; /* Add space between buttons */
        }
        
        /* Button Shared Styles */
        button,
        a {
            padding: 12px 20px;
            font-size: 16px;
            border: none;
            cursor: pointer;
            border-radius: 5px;
            text-decoration: none; /* Remove underline for links */
            transition: background-color 0.3s ease, box-shadow 0.3s ease; /* Smooth transition */
        }
        
        /* Go Back to Map Button */
        .back-to-map-btn {
            background-color: #28a745; /* Green color */
            color: white;
        }
        
        .back-to-map-btn:hover {
            background-color: #218838;
            box-shadow: 0 4px 10px rgba(40, 167, 69, 0.4); /* Add shadow on hover */
        }
        
        /* Edit Account Button */
        .edit-account-btn {
            background-color: #007bff; /* Blue color */
            color: white;
        }
        
        .edit-account-btn:hover {
            background-color: #0056b3;
            box-shadow: 0 4px 10px rgba(0, 123, 255, 0.4); /* Add shadow on hover */
        }
        
        /* Delete Account Button */
        .delete-account-btn {
            background-color: #dc3545; /* More neutral red */
            color: white;
        }
        
        .delete-account-btn:hover {
            background-color: #b21f2d; /* Darker red */
            box-shadow: 0 4px 10px rgba(220, 53, 69, 0.4); /* Add shadow on hover */
        }
        
        /* Logout Button at the top right */
        .logout-btn {
            position: absolute;
            top: 20px;
            right: 20px;
            padding: 10px 20px;
            background-color: #ff6f61; /* Softer red for a friendly look */
            color: white;
            font-size: 14px; /* Slightly smaller font size */
            font-weight: bold; /* Bold for emphasis */
            border-radius: 5px;
        }
        
        .logout-btn:hover {
            background-color: #e05246; /* Darker red on hover */
            box-shadow: 0 4px 10px rgba(255, 111, 97, 0.4); /* Add shadow on hover */
        }
        </style>
        
</head>
<body>
    <!-- Logout Button -->
    <form action="/logout" method="POST">
        <button class="logout-btn" type="submit">Logout</button>
    </form>

    <!-- Account Info Section -->
    <div class="account-info">
        <h2>User Information</h2>
        <p><strong>Name:</strong> <%= user.first_name %> <%= user.last_name %></p>
        <p><strong>Username:</strong> <%= user.username %></p> 
        <p><strong>Email:</strong> <%= user.email %></p>
        <p><strong>Home Address:</strong> 
            <%= user.home_address %>, <%= user.home_city %>, <%= user.home_state %> <%= user.home_zip %>, <%= user.home_country %>
        </p>

        <!-- Button Container -->
        <div class="button-container">
            <!-- Go Back to Map Button -->
            <a href="/map" class="back-to-map-btn">Go Back to Map</a>

            <!-- Edit Account Button -->
            <a href="/editAccount" class="edit-account-btn">Edit Account</a>

            <!-- Delete Account Button -->
            <form action="/deleteAccount" method="POST" onsubmit="return confirmDelete()">
                <button class="delete-account-btn" type="submit">Delete Account</button>
            </form>
        </div>
    </div>

    <script>
        // Confirmation dialog for account deletion
        function confirmDelete() {
            return confirm("Are you sure you want to delete your account? This action cannot be undone.");
        }
    </script>
</body>
</html>
